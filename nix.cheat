% nix

# Collect garbage
nix-collect-garbage --delete-old

# Search packages
nix search nixpkgs --offline 

# Search packages by binary
, --print-packages 

# Update package cache
nix-channel --update

# Open home-manager manpage
man home-configuration.nix
